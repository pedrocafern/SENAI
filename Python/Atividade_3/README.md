# 3º Atividade

Escreva um programa em python para calcular os valores de imc a partir de entradas de dados específicos
como peso e altura da pessoa. Não permita a entrada de valores que não possam ser números. O sistema deve
trazer o resultado escrito do IMC e não o cálculo. Não permita que o usuário entre com valores que não possam
ser números.

IMC = Peso[kg] ÷ (Altura²[m]) --> Isso é uma função

Op1 -> baixo peso < 18,5 --> If
Op2 -> 18,5 >= peso normal < 25 --> elif
Op3 -> 25 >= pre obesidade < 30 --> elif
Op4 -> 30 >= obesidade grau 1 < 35 --> elif
Op5 -> 35 >= obesidade grau 2 < 40 --> elif
Op6 -> Obesidade mórbida >= 40 --> else:
